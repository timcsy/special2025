import os; os.environ.setdefault("DJANGO_SETTINGS_MODULE", "special.settings"); import django; django.setup(); from score.models import Student, FoodManagement, FashionDesign, StylingDesign; print(f"學生總數: {Student.objects.count()}"); print(f"餐飲管理科成績數: {FoodManagement.objects.count()}"); print(f"流行服飾科成績數: {FashionDesign.objects.count()}"); print(f"整體造型特色班成績數: {StylingDesign.objects.count()}"); print("准考證號範例:"); for i, student in enumerate(Student.objects.all()[:5]): print(f"{student.admit_number}"); if i  break;  
